{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "User Schema",
    "type": "object",
    "properties": {
        "birthdate": {
            "type": "string",
            "format": "date-time",
            "description": "The user's birth date in timestamp format."
        },
        "created_time": {
            "type": "string",
            "format": "date-time",
            "description": "The timestamp when the user was created."
        },
        "display_name": {
            "type": "string",
            "description": "The display name of the user."
        },
        "email": {
            "type": "string",
            "format": "email",
            "description": "The email address of the user."
        },
        "photo_url": {
            "type": "string",
            "format": "uri",
            "description": "The URL of the user's profile photo."
        },
        "uid": {
            "type": "string",
            "description": "The unique identifier of the user."
        }
    },
    "required": [
        "birthdate",
        "created_time",
        "display_name",
        "email",
        "uid"
    ]
}
